- name: Install Apache
  apt: name={{ item }}
  with_items:
  - apache2
  - libapache2-mod-php5
  
- name: Configure Apache
  command: {{ item }}
  with_items: 
  - a2enmod rewrite
  - a2dissite 000-default
